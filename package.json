{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "nuxt-icon": "^0.1.8",
    "nuxt-windicss": "^2.6.0"
  },
  "dependencies": {
    "@nuxtjs/web-vitals": "^0.2.2",
    "@vueuse/core": "^9.10.0"
  }
}
